# Add python libraries required by your jobs (following standard python requirements.txt format)

# Libraries below needed for some of the example jobs.
# They can be removed if the example jobs are removed.
requests==2.32.2
transformers==4.38.0  # latest 4.36.2, to be tested with k8s setup
tensorflow==2.16.1  # latest 2.15.0, to be tested with k8s setup
sentencepiece==0.1.99  # to be tested with k8s setup
# add more as needed.
