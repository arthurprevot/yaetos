# For docker env. TODO: use it for AWS setup too, would involve splitting it in 2 req files (master and slave sides).

# Libs part of yaetos install (left here to specify versions)
pyyaml==5.4.1
awscli==1.20.41  # necessary to deploy to AWS Data Pipeline. Tried 1.19.7 but causes pb with botocore.
boto3==1.18.41
cloudpathlib==0.7.0
pandas==1.0.0
sqlalchemy==1.3.15
networkx==2.4
duckdb==0.4.0

# libs required for some functionalities but not required in import statements
openpyxl==3.0.9  # used to open excel files. TODO: put in a new requirement_extra.txt, since optional.
pymysql==0.9.3
psycopg2-binary==2.8.5
sqlalchemy-redshift==0.7.7
py4j==0.10.9.1  # TODO: check to remove.
# scikit-learn==1.1.2 # TODO: fix lib and re-enable when joblib code renabled in env_dispatchers.py, and move to yaetos install libs.
# botocore==1.8.7
# s3fs==2022.5.0  # for saving to S3 with pandas. TODO: more validation, impacts boto versions
# numpy  # req 1.21.0 doesn't work. needs to be forced to 1.18.5 instead of latest (1.19.2) to be compatible with koalas 1.3.0 (requiring <1.19)
# pyarrow==6.0.1  # latest 8.0.0 not available in env
# setuptools-rust==0.11.6  # latest 0.12.1
# cryptography==3.4.8

# Dev
ipdb==0.13.4
pytest==5.3.0
flake8==3.7.9
jupyterlab==3.4.7
jupyterlab-git==0.39.2

# Job specific
# statsmodels==0.9.0  # TODO: disabled since breaks in latest docker setup.
# kafka-python==1.4.7  # Uncomment if needed
# jsonschema==3.0.2 # needed by kafka_utils
# stripe==2.50.0  # Uncomment if needed
# soql==1.0.2  # Uncomment if needed (salesforce)
# simple-salesforce==1.10.1
# koalas==1.3.0
